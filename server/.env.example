# Postgres connection (used by index.js and migrate.js)
DATABASE_URL=postgres://lareprep:CHANGEME@127.0.0.1:5432/lareprep

# Bind address — keep on loopback; Caddy reverse-proxies /api/* to this
HOST=127.0.0.1
PORT=4001
